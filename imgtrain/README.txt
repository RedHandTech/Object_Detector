README

imgtrain is used to train a new object detector for use with dlib.

gather images that contain the object you want to detect

prepare them for training using imglab

once prepared run imgtrain and pass through the path to the directory of images as arg 1

pass through "help" for more info and optional arguments

the directory must contain the images and the xml that imglab creates

the xml must be called "training.xml"
